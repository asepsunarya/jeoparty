# Both are optional. In dev, leave empty and the client auto-targets the
# backend at the same hostname on port 4000 (so LAN peers Just Work).
# Override in production to pin a specific public URL.
# NEXT_PUBLIC_API_URL=https://api.example.com
# NEXT_PUBLIC_WS_URL=https://api.example.com
