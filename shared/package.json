{
  "name": "@jeoparty/shared",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "postinstall": "tsc -p tsconfig.json || true"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
